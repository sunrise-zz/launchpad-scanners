# secrets — never commit
pons/.env
pons/data/telegram.json

# runtime artifacts
*.log
*.pid
__pycache__/
*.pyc
.pytest_cache/
.venv/

# large raw datasets (regenerate with the collect scripts)
vlad/data/events.json
vlad/data/coins_rest.json
pons/data/launches.json
pons/data/early_swaps.json

# runtime artifacts — local scanner state & outcome tracking (regenerated live)
.DS_Store
**/data/events.jsonl
tracker/data/
pump/data/alerted_mints.txt
pump/data/.seeded
pons/data/discovery_cursor.json
