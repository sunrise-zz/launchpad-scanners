[pytest]
testpaths = tests
# tests/ is on sys.path so conftest's shared profiles can be imported by name;
# the scanners themselves are loaded by file path (three of them are scan.py).
pythonpath = tests
addopts = -q --strict-markers
